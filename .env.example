# --- REDIS CONFIGURATION ---
# Use an Upstash Redis URL or a local Redis instance
# Format: redis://[:password]@host:port
REDIS_URL=redis://127.0.0.1:6379

# --- NEXT PUBLIC CONFIG (Optional) ---
# NEXT_PUBLIC_MAFIA_CONTRACT_ADDRESS=0x...
# NEXT_PUBLIC_VERIFIER_CONTRACT_ADDRESS=0x...

# --- VOICE CHAT (MiroTalk SFU) ---
# Public URL for frontend
NEXT_PUBLIC_SFU_URL=https://mafia-voice.serveminecraft.net
# Private API secret for backend (must match SFU server's API_KEY_SECRET)
SFU_API_SECRET=your_sfu_api_secret_here
